version: '3.8'

services:
  server:
    build: ./server
    ports:
      - "3001:3001"
    environment:
      JWT_SECRET: dev-secret-change-in-production
      NODE_ENV: development
    env_file:
      - ./server/.env
    volumes:
      - ./server/uploads:/app/uploads

